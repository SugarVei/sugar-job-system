import type { AutofillRun } from '../../types/resumeAssistant';
import { ghostBtn, muted } from './styles';
export function RunDetailsDrawer({ run, onClose }: { run: AutofillRun | null; onClose: () => void }) { if (!run) return null; return <div role="dialog" aria-modal="true" style={{ position: 'fixed', inset: 0, background: 'rgba(25,20,12,.38)', zIndex: 100, display: 'grid', placeItems: 'center', padding: 20 }}><div style={{ background: '#fffdf8', borderRadius: 22, padding: 24, width: 'min(460px,100%)' }}><h2 style={{ marginTop: 0 }}>{run.origin_host}</h2><p style={muted}>状态：{run.status}；填写 {run.fields_filled}/{run.fields_total}，需要手动处理 {run.fields_manual} 项。</p><p style={muted}>适配器：{run.adapter_names.join('、') || '无'}<br/>错误码：{run.error_codes.join('、') || '无'}</p><button style={ghostBtn} onClick={onClose}>关闭</button></div></div>; }
